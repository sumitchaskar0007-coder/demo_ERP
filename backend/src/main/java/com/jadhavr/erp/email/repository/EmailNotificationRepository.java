package com.jadhavr.erp.email.repository;import com.jadhavr.erp.email.entity.EmailNotification;import com.jadhavr.erp.email.enums.*;import org.springframework.data.domain.*;import org.springframework.data.jpa.repository.*;import org.springframework.data.repository.query.Param;import java.time.LocalDateTime;import java.util.*;public interface EmailNotificationRepository extends JpaRepository<EmailNotification,Long>,JpaSpecificationExecutor<EmailNotification>{boolean existsByIdempotencyKey(String k);@Query(value="select * from email_notifications where (status='QUEUED' or (status='RETRY_PENDING' and next_retry_at<=now())) order by priority,created_at for update skip locked limit :limit",nativeQuery=true)List<EmailNotification> lockEligible(@Param("limit")int limit);List<EmailNotification> findByStatusAndProcessingStartedAtBefore(EmailStatus s,LocalDateTime t);}
