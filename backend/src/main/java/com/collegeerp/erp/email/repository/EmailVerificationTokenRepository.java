package com.collegeerp.erp.email.repository;import com.collegeerp.erp.email.entity.EmailVerificationToken;import org.springframework.data.jpa.repository.*;import java.util.*;public interface EmailVerificationTokenRepository extends JpaRepository<EmailVerificationToken,Long>{Optional<EmailVerificationToken>findByTokenHash(String h);List<EmailVerificationToken>findByUserIdAndUsedAtIsNullAndRevokedAtIsNull(Long id);}
