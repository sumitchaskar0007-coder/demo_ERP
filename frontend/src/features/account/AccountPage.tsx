import{useEffect,useState}from"react";import{Link}from"react-router-dom";import{toast}from"sonner";import{Card}from"@/components/common/Card";import{Input}from"@/components/common/Input";import{Button}from"@/components/common/Button";import{Loader}from"@/components/common/Loader";import{handleApiError}from"@/lib/handleApiError";import{getMyAccount,updateMyAccount,type AccountProfile}from"./api";export function AccountPage(){const[p,setP]=useState<AccountProfile|null>(null),[saving,setSaving]=useState(false);useEffect(()=>{getMyAccount().then(setP).catch(e=>toast.error(handleApiError(e).message))},[]);if(!p)return <Loader/>;const save=async()=>{if(p.fullName.trim().length<2)return toast.error("Full name is required");setSaving(true);try{setP(await updateMyAccount({fullName:p.fullName,phone:p.phone}));toast.success("Profile updated")}catch(e){toast.error(handleApiError(e).message)}finally{setSaving(false)}};return <div className="page-container"><h1 className="page-title">My Account</h1><p className="page-subtitle">Manage your personal profile and security.</p><Card className="mt-6 max-w-2xl p-6"><div className="grid gap-4 sm:grid-cols-2"><Input label="Full name" value={p.fullName} onChange={e=>setP({...p,fullName:e.target.value})}/><Input label="Email" value={p.email} disabled/><Input label="Phone" value={p.phone||""} onChange={e=>setP({...p,phone:e.target.value})}/><Input label="College" value={p.collegeName||"System"} disabled/></div><p className="mt-4 text-sm text-slate-500">Roles: {p.roles.join(", ")} · Status: {p.status}</p><div className="mt-5 flex gap-3"><Button loading={saving} onClick={save}>Update profile</Button><Link to="/account/change-password"><Button variant="secondary">Change password</Button></Link></div></Card></div>}
