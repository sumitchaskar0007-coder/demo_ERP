import{useEffect,useState}from"react";import{Link,useParams}from"react-router-dom";import{toast}from"sonner";import{Card}from"@/components/common/Card";import{Button}from"@/components/common/Button";import{Input}from"@/components/common/Input";import{StatusBadge}from"@/components/common/Badge";import{DataTable,type Column}from"@/components/table/DataTable";import{handleApiError}from"@/lib/handleApiError";import * as api from"./api";import type{FeeDashboardResponse,FeeTransactionResponse,PaymentResponse,PaymentStatus,StudentFeeAccountResponse}from"./types";
export function FeeSectionDashboardPage(){const[d,setD]=useState<FeeDashboardResponse|null>(null);useEffect(()=>{api.getFeeDashboard().then(setD).catch(e=>toast.error(handleApiError(e).message))},[]);if(!d)return <div className="page-container">Loading dashboard…</div>;const items=[["Fee Accounts",d.totalFeeAccounts],["Pending Payments",d.pendingPayments],["Verified Payments",d.verifiedPayments],["Rejected Payments",d.rejectedPayments],["Total Collected",`₹${d.totalCollectedAmount.toLocaleString("en-IN")}`],["Total Pending",`₹${d.totalPendingAmount.toLocaleString("en-IN")}`],["Today Collection",`₹${d.todayCollectedAmount.toLocaleString("en-IN")}`]];return <div className="page-container"><h1 className="page-title">Fee Section Dashboard</h1><div className="mt-5 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{items.map(([k,v])=><Card key={String(k)} className="p-5"><p className="text-xs font-bold uppercase text-slate-400">{k}</p><p className="mt-2 text-2xl font-black">{v}</p></Card>)}</div></div>}
export function FeeAccountsPage(){const[d,setD]=useState<StudentFeeAccountResponse[]>([]);const[q,setQ]=useState("");const load=()=>api.searchFeeAccounts({keyword:q||undefined,size:50}).then(x=>setD(x.content));useEffect(()=>{load();},[]);const cols:Column<StudentFeeAccountResponse>[]=[{key:"student",header:"Admission",render:r=><div><b>{r.admissionNumber}</b><p className="text-xs">{r.departmentName}</p></div>},{key:"total",header:"Total",render:r=>`₹${r.totalFee}`},{key:"paid",header:"Paid",render:r=>`₹${r.paidAmount}`},{key:"remaining",header:"Remaining",render:r=>`₹${r.remainingAmount}`},{key:"status",header:"Status",render:r=><StatusBadge status={r.status}/>},{key:"action",header:"Action",render:r=><Link to={`/fee-section/fee-accounts/${r.id}`}><Button variant="secondary">View</Button></Link>}];return <div className="page-container space-y-5"><h1 className="page-title">Student Fee Accounts</h1><Card className="flex gap-3 p-4"><Input placeholder="Search admission" value={q} onChange={e=>setQ(e.target.value)}/><Button onClick={load}>Search</Button></Card><DataTable columns={cols} data={d} rowKey={r=>r.id}/></div>}
export function FeeAccountDetailsPage(){const{feeAccountId}=useParams();const[a,setA]=useState<StudentFeeAccountResponse|null>(null);const[t,setT]=useState<FeeTransactionResponse[]>([]);useEffect(()=>{if(feeAccountId)Promise.all([api.getFeeAccountById(+feeAccountId),api.getFeeAccountTransactions(+feeAccountId)]).then(([x,y])=>{setA(x);setT(y)})},[feeAccountId]);if(!a)return <div className="page-container">Loading…</div>;return <div className="page-container space-y-5"><Card className="p-7"><div className="flex justify-between"><h1 className="page-title">{a.admissionNumber}</h1><StatusBadge status={a.status}/></div><div className="mt-5 grid gap-4 md:grid-cols-4">{[["Student",a.admissionReferenceNumber],["Department",a.departmentName],["Paid",`₹${a.paidAmount}`],["Remaining",`₹${a.remainingAmount}`]].map(([k,v])=><div key={k} className="rounded-xl bg-slate-50 p-4"><small>{k}</small><p className="font-bold">{v}</p></div>)}</div></Card><Card className="p-6"><h2 className="font-bold">Transactions</h2>{t.map(x=><div key={x.id} className="mt-3 flex justify-between border-t pt-3"><span>{x.transactionType.replaceAll("_"," ")}</span><b>₹{x.amount}</b></div>)}</Card></div>}
export function PaymentsPage(){const[d,setD]=useState<PaymentResponse[]>([]);const[s,setS]=useState<PaymentStatus|"">("PENDING");const load=()=>api.searchPayments({status:s||undefined,size:50}).then(x=>setD(x.content));useEffect(()=>{load();},[s]);const cols:Column<PaymentResponse>[]=[{key:"student",header:"Student",render:r=><div><b>{r.studentName}</b><p className="text-xs">{r.admissionNumber}</p></div>},{key:"amount",header:"Amount",render:r=>`₹${r.amount}`},{key:"reference",header:"Reference",render:r=>r.transactionReference},{key:"mode",header:"Mode",render:r=>r.paymentMode},{key:"status",header:"Status",render:r=><StatusBadge status={r.status}/>},{key:"action",header:"Action",render:r=><Link to={`/fee-section/payments/${r.id}`}><Button variant="secondary">Review</Button></Link>}];return <div className="page-container space-y-5"><h1 className="page-title">Fee Payments</h1><Card className="p-4"><select className="h-10 rounded-xl border px-3" value={s} onChange={e=>setS(e.target.value as PaymentStatus|"")}><option value="">All statuses</option>{["PENDING","VERIFIED","REJECTED"].map(x=><option key={x}>{x}</option>)}</select></Card><DataTable columns={cols} data={d} rowKey={r=>r.id}/></div>}
export function PaymentDetailsPage(){const{paymentId}=useParams();const[p,setP]=useState<PaymentResponse|null>(null);const load=()=>{if(paymentId)api.getPaymentById(+paymentId).then(setP);};useEffect(()=>{load();},[paymentId]);if(!p)return <div className="page-container">Loading…</div>;const verify=()=>api.verifyPayment(p.id).then(()=>{toast.success("Payment verified");load()}).catch(e=>toast.error(handleApiError(e).message));const reject=()=>{const reason=window.prompt("Enter rejection reason (minimum 5 characters)");if(reason&&reason.length>=5)api.rejectPayment(p.id,reason).then(()=>{toast.success("Payment rejected");load()})};return <div className="page-container"><Card className="mx-auto max-w-3xl p-7"><div className="flex justify-between"><h1 className="page-title">Payment Review</h1><StatusBadge status={p.status}/></div><div className="mt-6 grid gap-4 md:grid-cols-2">{Object.entries({Student:p.studentName,Admission:p.admissionNumber,Amount:`₹${p.amount}`,Mode:p.paymentMode,Reference:p.transactionReference,"Payment Date":p.paymentDate}).map(([k,v])=><div key={k} className="rounded-xl bg-slate-50 p-4"><small>{k}</small><p className="font-bold">{v}</p></div>)}</div><a href={p.proofUrl} target="_blank" rel="noreferrer" className="mt-5 inline-block font-semibold text-blue-600">Open Payment Proof</a>{p.status==="PENDING"&&<div className="mt-6 flex gap-3"><Button onClick={verify}>Verify Payment</Button><Button variant="danger" onClick={reject}>Reject Payment</Button></div>}</Card></div>}

