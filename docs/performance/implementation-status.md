# 5,000-user hardening implementation status

Status date: 2026-07-29

This table is deliberately conservative. “Completed locally” means source and
tests exist in the working tree. It does **not** mean the change is committed,
deployed, enabled in AWS, production-ready or capacity-proven. No AWS write,
deployment, DNS change, secret update or shared-environment load test is
authorized by this document.

Every “Live AWS” entry below is from the last detailed 2026-07-28 inventory.
The 2026-07-29 refresh could not authenticate because the configured root login
expired; re-verify through least-privilege SSO/assumed-role access before
planning.

## Application, infrastructure and validation matrix

| Requirement                           | Status                                                       | Local evidence / tests                                                                                                                                                                        | Live AWS effect                                                                      | Remaining risk and next action                                                                                                              |
| ------------------------------------- | ------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------- |
| AWS/repository baseline               | Completed for 2026-07-28; refresh blocked                    | Detailed read-only inventory is recorded in `5000-user-readiness-audit.md`; the 2026-07-29 refresh failed because the configured root login expired                                           | None                                                                                 | Establish least-privilege SSO/assumed-role access and repeat the complete read-only inventory before planning                               |
| Distributed user-aware rate limiting  | Completed locally; not deployed                              | Redis-backed user/account-plus-trusted-IP limiting, consistent 429/`Retry-After`, proxy resolution and automated tests; included in the 257-test backend verification                         | Live WAF/application behavior is unchanged                                           | Validate across multiple ECS tasks and shared-NAT synthetic users; review a scoped WAF plan                                                 |
| WAF login-wave protection             | Partially prepared; not applied                              | Local Terraform makes the edge IP ceiling configurable and retains application user/account limits; it does not make WAF identity-aware                                                       | Live auth rule remains 300 requests/5 minutes/source IP                              | Review CloudFront/WAF metrics and a scoped high-ceiling edge rule in isolated staging; retain brute-force controls                          |
| Authorization snapshot                | Completed locally; not deployed                              | Minimal Redis snapshot, mutation invalidation, DB fallback, metrics and automated tests; included in backend verification                                                                     | Live request path is unchanged                                                       | Exercise stale-cache and Valkey-outage behavior in isolated staging                                                                         |
| Notice SSE and unread fallback        | Completed locally; not deployed                              | Authenticated SSE/unread-count API, Redis Pub/Sub fan-out, heartbeats, frontend reconnect/visibility behavior and 90-second fallback; backend and frontend tests pass                         | Live frontend/backend remain unchanged                                               | Verify long-lived connections through CloudFront/ALB and task replacement                                                                   |
| Database query optimization           | Partially completed locally                                  | Scoped/batched/aggregate queries and `V22`; clean PostgreSQL 16 applied all 20 migrations through `V25` and passed Hibernate validation in 5.5s with the concurrent-index Flyway lock setting | No staging/external RDS migration has run                                            | Complete the broad query review and production-sized synthetic `EXPLAIN (ANALYZE, BUFFERS)`; obtain RDS-owner migration approval            |
| Admission-document presigned transfer | Completed locally; not deployed                              | Tenant-bound presign/complete/download flow, checksum/type/size validation, private keys, upload state in `V23`, frontend PUT/progress/abort/fallback and automated tests                     | No bucket/CORS/KMS/application change is live                                        | Legacy multipart paths remain for compatibility; verify exact S3 CORS/encryption and tenant isolation in staging                            |
| Other file transfers                  | Partially completed                                          | Shared private storage remains, but not every payment/photo/branding transfer has moved to a presigned contract                                                                               | None                                                                                 | Inventory all remaining backend-buffered upload/download paths before closing the broad direct-S3 requirement                               |
| Malware protection/quarantine         | Partially prepared; disabled                                 | Review-only GuardDuty Malware Protection for S3 Terraform, event alarms and tagging are present with `malware_protection_enabled=false`                                                       | No plan or scan is active                                                            | Add and test the application promotion/quarantine consumer, obtain current cost and recipient approval, then request staging apply approval |
| Async email                           | Completed locally; not deployed                              | SQS ID-only message contract, publisher/consumer/recovery, delivery state, retry/visibility handling and duplicate-prevention tests; included in backend verification                         | No queues or worker exist; SES remains sandboxed                                     | Obtain SES production access, alert recipient, cost and staging approval; verify crash-after-send duplicate behavior operationally          |
| Async report exports                  | Partially completed locally; not deployed                    | Durable `V25` jobs, status/download API, tenant-safe private S3 result, SQS/DB recovery worker, retry/idempotency and tests for CSV admissions/fees/attendance/students                       | No queue, worker or result storage change is live                                    | Existing synchronous exports remain; migrate/implement the broader PDF/Excel workflows and verify cleanup/expiry                            |
| Optimistic locking                    | Completed locally for selected mutable records; not deployed | `@Version` on admission forms and fee structures, additive `V24`, HTTP 409 mapping, overlapping-transaction tests and clean local PostgreSQL migration validation                             | No staging/external RDS migration has run                                            | Confirm API/client conflict UX and add only evidence-backed aggregates later                                                                |
| Async ECS worker                      | Prepared locally; disabled                                   | Separate task definition/service, queue-specific IAM, independent Hikari budget and API/worker feature flags; workflow updates the exact worker digest with compensating API/worker rollback  | `async_queues_enabled=false`; no worker is live                                      | Validate cancellation/reconciliation behavior, then stage with queue/DB/SES approvals                                                       |
| ECS 2–12 scaling and health controls  | Prepared locally; not applied                                | ECS Terraform includes min 2, max 12, optional peak scheduling, CPU/memory/request scaling, circuit breaker and DB-connection-budget guards                                                   | Live staging remains min 2/max 6; production ECS does not exist                      | Review current cost, external DB budget and staging plan; do not apply yet                                                                  |
| External production network           | Blocked/external                                             | Production Terraform consumes external VPC and subnet IDs and does not create a production VPC                                                                                                | Latest inventory found no production VPC                                             | Network owner must supply the VPC plus at least two approved public/backend subnets before a production plan                                |
| External production RDS               | Blocked/external                                             | Terraform consumes all required external values; local PostgreSQL 16 validated `V22`–`V25`; staging-managed RDS behavior is unchanged                                                         | Only staging RDS exists; no production values/secrets exist                          | RDS owner supplies values, reviews the Flyway lock setting/migrations, and later allows 5432 only from output `backend_security_group_id`   |
| Production ECS backend                | Blocked                                                      | ECS is the authoritative target; production example starts at zero tasks until `production_database_access_ready=true`                                                                        | No production ECS service/ALB exists                                                 | Resolve identity, external network/RDS, Terraform state ownership, current quote and explicit staging/production gates                      |
| Legacy EC2 path                       | Review-only; not an approved production target               | `infra/terraform-ec2` and a manual workflow exist for legacy compatibility review                                                                                                             | Standalone EC2 instances are unmanaged and not HA                                    | Do not use this single-instance path for production or the 5,000-user target                                                                |
| Monitoring and notifications          | Partially prepared; not applied                              | Terraform contains capacity dashboard and additional ALB/ECS/RDS/Valkey/SQS/SES alarms                                                                                                        | Live AWS still has 14 alarms, no dashboard, no SQS alarms and zero SNS subscriptions | Complete missing application-level signals and obtain the real alert recipient; never invent one                                            |
| CI/CD safety                          | Prepared locally; not executed                               | CI now runs PostgreSQL migration validation; manual ECS workflow verifies an approved exact-digest migration, API and optional worker, with rollback/reconciliation guards                    | No workflow has been triggered; GitHub authentication/configuration is incomplete    | Configure required reviewers/branch protections; runner hard-kill and >30-minute migration reconciliation remain operational risks          |
| Backend verification                  | Completed locally                                            | JDK 17 Maven verify: 257 tests; clean PostgreSQL 16 applied/validated all 20 migrations through `V25` plus Hibernate schema validation                                                        | None                                                                                 | Repeat in protected CI and isolated staging with external-contract-equivalent PostgreSQL/Valkey/SQS                                         |
| Frontend verification                 | Completed locally                                            | 29 unit tests, lint, format check and production build passed                                                                                                                                 | None                                                                                 | Repeat in CI; investigate the existing large spreadsheet bundle warning separately                                                          |
| Docker image build                    | Blocked locally                                              | Docker daemon is not running; CI build/scan steps are prepared                                                                                                                                | None                                                                                 | Start an approved Docker runtime or rely on the protected CI gate after code review                                                         |
| 500/1,000/2,500/5,000 tests           | Not run                                                      | Guarded k6 project and runbook exist; k6 is not installed locally                                                                                                                             | None                                                                                 | Create an isolated target and 5,000 unique synthetic users, approve spend, then pass each gate in order                                     |

The system remains **not proven for 5,000 concurrent users**.
