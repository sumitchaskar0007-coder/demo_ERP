# This key is intentionally different from historical production and staging
# keys. It is exclusively for the separately owned ECS production stack.
bucket       = "college-erp-terraform-state-814645955631-ap-south-1"
key          = "production-v2/terraform.tfstate"
region       = "ap-south-1"
encrypt      = true
use_lockfile = true
